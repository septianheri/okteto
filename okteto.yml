build:
  api:
    context: api
deploy:
  - apt-get install nodejs -y
  - npm run
dev:
  api:
    command: ["nodemon index.js"]
    forward:
      - 8080:8080
      - 9229:9229
    sync:
      - api:/usr/src/app
